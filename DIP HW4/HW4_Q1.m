%function ?????????????%
clear all;

RGB = imread('pears.png');
subplot(121),imshow(RGB),title('orignal');


for i = 1:3
    A = RGB(:, :, i);
    [hang,col] = size(A);% I = zeros(hang,col,3);
    for j = 1:hang
        B = A(j,:);
        C = double(B);
        [M ,N] = butter(8,0.5);  %butterworth ?????????????8?????
                                 % 0.5??????,????????sqrt?1/2??
                                 %???0-1????1?Nyquist ??,?????????%
            
        D = filter(M,N,C);
        A(j,:) = D;
    end
    RGB(:,:,i) =A;
end
%J = uint8(I);%
subplot(122),imshow(RGB),title('butter image');%??????%